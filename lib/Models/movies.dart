class Movies {
  int? id, is_upcoming, is_free, producer_id, language_id, movie_type_id;
  String? title,
      slug,
      release_date,
      description,
      directors,
      actors,
      trailer_url,
      meta_title,
      meta_description,
      meta_keywords,
      producer_name,
      language_name,
      movie_type,profile_pic,poster_pic;
}
